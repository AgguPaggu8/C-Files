#include <stdio.h>

int main(){
// const, adding this before the variable makes it so you cant change it by somewhere else//
// it is a good practice to keep the conctant variable name in ALL CAPS //
    const float PI = 3.1494853;

    printf("%f", PI);


    return 0;
}